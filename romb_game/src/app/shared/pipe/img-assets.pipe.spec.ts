import { ImgAssetsPipe } from './img-assets.pipe';

describe('ImgAssetsPipe', () => {
  it('create an instance', () => {
    const pipe = new ImgAssetsPipe();
    expect(pipe).toBeTruthy();
  });
});
