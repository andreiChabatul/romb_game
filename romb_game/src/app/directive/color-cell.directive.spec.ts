import { ColorCellDirective } from './color-cell.directive';

describe('ColorCellDirective', () => {
  it('should create an instance', () => {
    const directive = new ColorCellDirective();
    expect(directive).toBeTruthy();
  });
});
